Replace smartco.svg and mufg.svg with the official assets (SVG preferred, scales
cleanly on a projector). Keep the same filenames and the app needs no code change.
These are on-brand placeholders only.
